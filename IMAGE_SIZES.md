# 画像サイズの詳細

## compare_th_if で使用している画像

### カバー画像（Cover Image）
- **画像**: `Lenna_grayscale_256.bmp`
- **サイズ**: **256×256ピクセル**
- **用途**: 秘密画像を埋め込む元の画像

### 秘密画像（Secret Images）
以下の8種類の画像を256×256のカバー画像に埋め込んでいます：

| 画像名 | サイズ | 説明 |
|--------|--------|------|
| `secret_grayscale_64x64.bmp` | **64×64** | 全画素値127のグレースケール画像 |
| `secret_grayscale_128x128.bmp` | **128×128** | 全画素値127のグレースケール画像 |
| `secret_grayscale_256x256.bmp` | **256×256** | 全画素値127のグレースケール画像 |
| `airplane256.bmp` | **256×256** | 飛行機の画像 |
| `barbara256.bmp` | **256×256** | Barbaraの画像 |
| `boat256.bmp` | **256×256** | ボートの画像 |
| `cameraman256.bmp` | **256×256** | カメラマンの画像 |
| `mandrill256.bmp` | **256×256** | マンドリルの画像 |

## 埋め込み方法

### 閾値調整埋め込み手法（Threshold Adjust）
- カバー画像を直交変換（Hadamard変換）して係数行列を得る
- 係数行列の**左上から順番に**秘密画像の画素値を埋め込む
- 埋め込み領域: 係数行列の `[0:secretImageHeight][0:secretImageWidth]` の範囲

### 具体例
- **64×64の秘密画像**: 係数行列の左上64×64の領域に埋め込む
- **128×128の秘密画像**: 係数行列の左上128×128の領域に埋め込む
- **256×256の秘密画像**: 係数行列の左上256×256の領域（全体）に埋め込む

## 制約
- カバー画像のサイズは256×256
- 秘密画像のサイズはカバー画像以下である必要がある
- 係数行列はカバー画像と同じサイズ（256×256）であるため、秘密画像は最大256×256まで埋め込める


